using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Weapon_Model : MonoBehaviour {

	// Update is called once per frame
	void Update ()
    {
        transform.Rotate(0.0f,1,0.0f);
    }
}
